postgres:
  pool:
    ConnConfig:
      Host: "postgres-skills"
      Port: 5432
      Database: "tsuskills_skills"
      User: "postgres"
      Password: "postgres"
      SSLMode: "disable"
      ConnectTimeout: 5
    MaxConns: 10
    MinConns: 2
    MaxConnLifetime: 30m
    MaxConnIdleTime: 5m
    HealthCheckPeriod: 30s
  ConnectRetries: 5
  ConnectRetryDelay: 3s
  Schema: "skills"
  MigrationsPath: "internal/infra/postgres/migrations"

server:
  host: 0.0.0.0
  port: 8082
  ReadTimeout: 15s
  WriteTimeout: 15s
  IdleTimeout: 60s
  ShutDownTimeOut: 30s

logger:
  logger:
    Level: "info"
    Development: false
    Encoding: "json"
    OutputPaths: ["stdout"]
    ErrorOutputPaths: ["stderr"]
    EncoderConfig:
      MessageKey: "message"
      LevelKey: "level"
      TimeKey: "timestamp"
